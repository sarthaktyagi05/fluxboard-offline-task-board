# fluxboard-offline-task-board
FluxBoard is an offline-first task management board built with React 18 and TypeScript. It demonstrates normalized state architecture, O(1) task lookup, undo/redo history stack, debounced localStorage persistence, and optimized rendering for large datasets.
